# Shared infrastructure packages (Phase 2)

- `configs/` — Viper configuration loading
- `jwt/` — Token generation and validation
- `redis/` — Cache client adapter
- `rabbitmq/` — Event publisher/consumer
- `websocket/` — Realtime hub
- `validator/` — Request validation helpers
- `response/` — Standard API envelope helpers
