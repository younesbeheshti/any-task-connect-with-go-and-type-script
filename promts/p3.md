# Continue Implementation - Chat Refresh, Task Applications & Assignment Flow

Implement the following features and ensure that both the frontend and backend are fully synchronized. Do not use mock data. Everything should work through the existing APIs or by creating the necessary backend endpoints.

---

# 1. Auto Refresh Chat and everywhere that needed

Implement automatic chat synchronization.

## Requirements

* Refresh chat messages every **5 seconds**.
* when agent apply for any task
* Only fetch new messages if possible (avoid unnecessary full reloads).
* Preserve the user's current scroll position whenever appropriate.
* If the user is already at the bottom of the chat, automatically scroll to the newest message.
* Do not create duplicate messages.
* Keep frontend state synchronized with the backend.

---

# 2. Unread Message Badge

Whenever someone sends a new message:

* Show an unread message badge on the Task list/card.
* Display the number of unread messages.
* The badge should disappear or reset after the user opens the chat.
* Update unread counts automatically during the 5-second refresh cycle.
* Synchronize unread message counts with the backend whenever possible.

---

# 3. Agent Application Button

When an **Agent** views an available task:

* Display an **Apply** button.
* The button should only be visible if:

  * the task is open,
  * the agent has not already applied,
  * the task has not yet been assigned.
* Clicking **Apply** should:

  * create a task application,
  * prevent duplicate applications,
  * update the UI immediately,
  * disable or replace the button with an "Applied" state.

Implement all required backend validation and endpoints if they do not already exist.

---

# 4. Requester - View Applications

When a **Requester** opens one of their own tasks:

* Show a complete list of agents who applied.
* Each application should display useful information such as:

  * Agent name
  * Rating (if available)
  * Profile/avatar (if available)
  * Application date
  * Any additional information already stored

Currently, clicking **Agent Applications** does nothing.

Fix this functionality so the application list loads correctly from the backend.

---

# 5. Assign Task to an Agent

The requester must be able to choose exactly one applicant.

Implement the complete assignment workflow.

Requirements:

* Display a **Select / Assign** button for each applicant.
* Require confirmation before assigning.
* After assignment:

  * update the task status,
  * store the assigned agent,
  * notify the frontend immediately,
  * remove the task from other agents' available task lists,
  * prevent additional applications,
  * disable further assignments,
  * update all related UI.

Ensure all business rules are enforced on the backend.

---

# 6. Backend Synchronization

Review the entire task application workflow.

Verify that:

* APIs exist for every frontend action.
* Request and response models match.
* Error handling is implemented.
* Authorization rules are enforced.
* Duplicate applications are prevented.
* Assigned tasks cannot receive new applications.
* Only the requester can assign an agent.
* Only one agent can be assigned to a task.

Create or modify backend endpoints wherever necessary.

---

# 7. Frontend Improvements

Review all related pages and components.

Ensure:

* Proper loading states
* Empty states
* Success notifications
* Error notifications
* Disabled buttons while requests are in progress
* Proper state updates without requiring a manual page refresh

---

# Final Verification

Before finishing:

* Verify the complete flow works end-to-end:

  1. Agent views available task.
  2. Agent applies.
  3. Requester sees the application list.
  4. Requester selects one agent.
  5. Task becomes assigned.
  6. Other agents can no longer apply.
  7. Chat refreshes automatically every 5 seconds.
  8. Unread message badges update correctly.
  9. Frontend and backend remain fully synchronized.

Finally, provide a summary including:

* Files modified
* Backend endpoints added or changed
* Database changes (if any)
* Frontend components updated
* Remaining improvements or known limitations
